{
  "name": "@opportunity-scout/infra",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "synth": "cdk synth",
    "deploy": "cdk deploy --require-approval broadening"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.261.0",
    "constructs": "^10.7.0"
  },
  "devDependencies": {
    "@types/node": "^24.13.3",
    "aws-cdk": "^2.1132.0",
    "esbuild": "^0.28.1",
    "tsx": "^4.23.1",
    "typescript": "^6.0.3"
  }
}
